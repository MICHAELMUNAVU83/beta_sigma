# Workflows

## New Contributor Setup

1. Run `mix setup`.
2. Start `mix phx.server`.
3. Log in as admin or staff from the seeded credentials in `README.md`.
4. Use `/app/projects` as the internal starting point.

